"""Reusable model, logging, loss, and checkpoint components.

Modules are imported explicitly so reward/feature utilities remain usable in
environments that do not install the optional PyTorch training dependency.
"""
