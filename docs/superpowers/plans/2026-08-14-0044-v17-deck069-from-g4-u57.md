# 0044 V17 Deck 069 From G4 U57 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Start a new formal rollout-only PPO version from the immutable V15 U57 model-only checkpoint, with exact focal deck 069 and immutable Champion-G3 opponents sampled Meta-first from the expanded live deck pool.

**Architecture:** Promote decks 068 and 069 into the formal training deck pool, generalize Meta-first scheduling from the historical 67-card pool to the registry-defined contiguous 001–069 pool, and keep the historical Benchmark V2 schedule explicitly frozen to 001–067. A V17 wrapper pins the parent checkpoint/hash, focal identity/class, Champion-G3 identity, unchanged V15 hyperparameters, fresh optimizer, no PFSP, and no evaluate.

**Tech Stack:** Python 3.11, PyTorch PPO, CUDA Engine 2.0, pytest, W&B online.

## Global Constraints

- Champion-G3 remains immutable and is the only opponent policy.
- Opponent decks are the expanded exact pool 001–069 and use Meta-first, Meta-within-deck-balanced sampling.
- Focal deck is exactly 069, routed only through own-deck Meta class 03.
- Source weights are V15 `update-000057.pt`; optimizer is fresh and the new local update starts at U0.
- PPO and learning-rate settings remain identical to V15; periodic evaluate and PFSP remain disabled.
- No official engine source file may be modified.
- The new version must use unused `artifact/`, `checkpoint/`, `tensorboard/`, and `wandb/` paths and a new stable W&B run ID.

---

### Task 1: Expand the formal pool without changing Benchmark V2

**Files:**
- Modify: `train/0044_g2_dragapult_policy_option_lora/assets/decks/registry.json`
- Modify: `train/0044_g2_dragapult_policy_option_lora/assets.py`
- Modify: `train/0044_g2_dragapult_policy_option_lora/league/meta_balanced.py`
- Modify: `train/0044_g2_dragapult_policy_option_lora/training/run_v1.py`
- Modify: `train/0044_g2_dragapult_policy_option_lora/evaluation/benchmark_v2_schedule.py`
- Test: `train/0044_g2_dragapult_policy_option_lora/tests/test_v17_deck069_restart.py`

**Interfaces:**
- Consumes: `AssetRegistry.decks`, own-archetype mappings, and Meta-balanced scheduling.
- Produces: formal train/opponent pool 001–069 while Benchmark V2 remains frozen to its historical 001–067 pool and hashes.

- [ ] **Step 1: Write a failing boundary test**

Assert that 068/069 are formal training decks, the Meta-first scheduler covers all 69 exact decks, and Benchmark V2 still emits only historical opponent IDs 001–067.

- [ ] **Step 2: Run the boundary test and verify failure**

Run: `pytest -q train/0044_g2_dragapult_policy_option_lora/tests/test_v17_deck069_restart.py`
Expected: FAIL because the current registry marks 068/069 evaluation-only and the scheduler hard-codes 67 decks.

- [ ] **Step 3: Implement the minimal fixed-focal boundary**

Assign 068/069 the formal training role, generalize Meta-first validation to the exact contiguous IDs supplied by the mapping set, rename the training sampling mode to `meta_balanced_training_pool`, and hard-pin Benchmark V2 to IDs 001–067 independently of roles.

- [ ] **Step 4: Run the boundary and existing scheduling tests**

Run: `pytest -q train/0044_g2_dragapult_policy_option_lora/tests/test_v17_deck069_restart.py train/0044_g2_dragapult_policy_option_lora/tests/test_v12_g3_to_g4.py train/0044_g2_dragapult_policy_option_lora/tests/test_frozen_schedule.py`
Expected: PASS.

### Task 2: V17 pinned launch wrapper and readiness audit

**Files:**
- Create: `train/0044_g2_dragapult_policy_option_lora/training/run_v17.py`
- Modify: `train/0044_g2_dragapult_policy_option_lora/tests/test_v17_deck069_restart.py`

**Interfaces:**
- Consumes: V15 U57 checkpoint, Champion-G3 portable base, V15/V12 hyperparameter constants, and Task 1 fixed-focal authorization.
- Produces: `readiness()` and `launch()` for `V17_g4_u57_deck069_champion_g3_rollout_only`.

- [ ] **Step 1: Extend the test with pinned identity assertions**

Assert exact version, parent version/update/hash, focal `069`, own class `03`, opponent singleton `Champion-G3`, opponent decks 001–069, `rollout_games=512`, logical minibatch 4096, microbatch 768, fresh optimizer, no PFSP, and no evaluate.

- [ ] **Step 2: Run the test and verify the module is missing**

Run: `pytest -q train/0044_g2_dragapult_policy_option_lora/tests/test_v17_deck069_restart.py`
Expected: FAIL because `training.run_v17` does not exist.

- [ ] **Step 3: Implement readiness and launch**

Create the wrapper, hard-check the source checkpoint SHA/update/version and deck 069 asset/mapping, materialize Champion-G3 for identity audit, reject used version paths, and call `run()` with fixed focal 069, expanded Meta-balanced opponent pool, and the unchanged V15 optimizer configuration.

- [ ] **Step 4: Run readiness and tests**

Run: `python3 -m train.0044_g2_dragapult_policy_option_lora.training.run_v17`
Run: `pytest -q train/0044_g2_dragapult_policy_option_lora/tests/test_v17_deck069_restart.py`
Expected: readiness status `READY_AWAITING_USER_LAUNCH`; tests PASS.

### Task 3: Design synchronization and launch verification

**Files:**
- Modify: `experiments/0044_g2_dragapult_policy_option_lora/DESIGN.md`
- Modify: `experiments/0044_g2_dragapult_policy_option_lora/DESIGN.html`

**Interfaces:**
- Consumes: V17 readiness output and current deck/policy identities.
- Produces: authoritative documentation and a live formal W&B run.

- [ ] **Step 1: Update both design documents**

Document V17 as a deck-069 specialization from V15 U57, fresh optimizer/local U0, own Meta class 03, frozen Champion-G3 opponents with decks 001–069, unchanged V15 PPO parameters, frozen historical Benchmark V2 pool, and rollout-only monitoring.

- [ ] **Step 2: Run focused regression tests**

Run: `pytest -q train/0044_g2_dragapult_policy_option_lora/tests/test_v17_deck069_restart.py train/0044_g2_dragapult_policy_option_lora/tests/test_assets.py train/0044_g2_dragapult_policy_option_lora/tests/test_own_archetype_taxonomy_v2.py train/0044_g2_dragapult_policy_option_lora/tests/test_v12_g3_to_g4.py`
Expected: PASS.

- [ ] **Step 3: Launch formal training**

Run: `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True python3 -m train.0044_g2_dragapult_policy_option_lora.training.run_v17 --launch-formal`
Expected: U0 model-only checkpoint, online W&B initialization, 512 valid CUDA rollout games, then a successful U1 optimizer update and checkpoint.

- [ ] **Step 4: Audit the first update**

Verify status, checkpoint identity, opponent policy routing, focal deck counts, own Meta class, rollout errors/unfinished, CUDA feature D2H, throughput, losses, KL, and W&B sync before leaving the long run active.

### Task 4: Telemetry closed-enum retry

**Files:**
- Modify: `train/0044_g2_dragapult_policy_option_lora/telemetry.py`
- Create: `train/0044_g2_dragapult_policy_option_lora/training/run_v18.py`
- Test: `train/0044_g2_dragapult_policy_option_lora/tests/test_telemetry.py`

**Interfaces:**
- Consumes: the V17 failure at the boundary after U1 checkpoint and before canonical metrics.
- Produces: telemetry support for `meta_balanced_training_pool` and a clean V18 retry from original V15 U57.

- [x] **Step 1: Reproduce the rejected sampling mode with a 512-row telemetry test**
- [x] **Step 2: Admit the new mode to the same non-PFSP Meta-balanced telemetry branch**
- [x] **Step 3: Preserve V17 as failed and exclude its metric-less U1 as a parent**
- [x] **Step 4: Create V18 with a new W&B identity and fresh on-policy restart from V15 U57**
