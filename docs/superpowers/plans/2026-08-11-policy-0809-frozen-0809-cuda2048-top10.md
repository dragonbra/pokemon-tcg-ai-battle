# Policy-0809 Frozen-0809 CUDA-2048 Top-10 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Execute this plan task-by-task in the current session. Subagent execution is disabled by the repository session contract. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce protocol-compliant Frozen-0809 CUDA-2048 official-engine evidence for exact Frozen decks 001 through 010 with Policy-0809 as both focal base and frozen opponent policy.

**Architecture:** Extend the resident CUDA benchmark with a portable-candidate mode that loads the FP16 artifact as FP32 runtime tensors and emits a fail-closed deployment audit. Add a separate Policy-0809 driver so the existing Policy-0806 reports and cache semantics remain immutable; the driver resolves both policy identities before execution, builds deterministic 8 x 256 schedules, invokes the CUDA benchmark, validates every terminal result, and publishes per-deck plus aggregate evidence.

**Tech Stack:** Python 3.11, PyTorch, existing Semantic0031 portable runtime, official CUDA engine extension, JSON/HTML reporting, unittest/pytest.

## Global Constraints

- Read and obey `docs/rl/RL_PROMOTE_CHAMPION_FROZEN_POLICY_PROTOCOL_V1.md` before implementation or execution.
- Candidate contract is exactly `kaggle_fp16_storage_fp32_runtime_v1`; missing or non-PASS deployment audit aborts before official games.
- Opponent is the complete immutable `Policy-0809`, independently resolved from the focal portable candidate.
- The run uses master seed `341512806`, 256 immutable slots, replicas `0..7`, seeded toss, and Agent context-41 first-player choice.
- Each deck must finish exactly 2,048 terminal games with zero engine errors and zero unfinished games.
- Preserve separate Frozen-0806 and Frozen-0809 evidence namespaces and never relabel historical raw-FP32 results.
- Do not modify `engine/source/`.

---

### Task 1: Portable CUDA candidate identity gate

**Files:**
- Modify: `engine_cuda/tools/benchmark_0037_cuda_resident_refill.py`
- Modify: `engine_cuda/tools/benchmark_0037_update32_cuda_rollout.py`
- Test: `engine_cuda/tests/test_policy_0809_cuda_evaluation.py`

**Interfaces:**
- Consumes: a self-contained 0031 candidate package, source FP32 checkpoint, frozen opponent checkpoint, and focal exact deck.
- Produces: `actor_mode="portable"` plus a `candidate_deployment_identity_audit` manifest with source, portable, and effective hashes and `PASS` status.

- [x] Write tests that reject FP32 portable storage, reject non-FP32 runtime tensors, and accept the audited FP16-storage/FP32-runtime package.
- [x] Run the focused tests and confirm they fail before implementation.
- [x] Implement portable actor loading and fail-closed identity validation before CUDA engine construction.
- [x] Persist the audit and independent opponent checkpoint provenance in benchmark JSON.
- [x] Run the focused tests and existing resident-router tests.

### Task 2: Frozen-0809 formal driver and reports

**Files:**
- Create: `engine_cuda/tools/evaluate_policy_0809_cuda.py`
- Test: `engine_cuda/tests/test_policy_0809_cuda_evaluation.py`

**Interfaces:**
- Consumes: deck numbers, Policy-0809 registry entry, Frozen catalog, portable candidate packages, and the resident benchmark.
- Produces: resumable per-deck result JSON, full per-game evidence, per-deck HTML, aggregate manifest, and aggregate HTML.

- [x] Write schedule and preflight tests for exactly 2,048 unique jobs, replicas `0..7`, requested decks `001..010`, full Policy-0809 identity, and hard-fail audit behavior.
- [x] Run the focused tests and confirm the new driver assertions fail before implementation.
- [x] Implement immutable package materialization for each exact deck using the registered Policy-0809 source checkpoint.
- [x] Implement preflight identity resolution and candidate deployment audit validation before subprocess execution.
- [x] Implement resumable execution that only accepts exact schedule/hash/model/audit matches.
- [x] Implement reports containing all required candidate, opponent, seed, seat-choice, terminal, and error fields.
- [x] Run the focused tests and compile/import checks.

### Task 3: Execute decks 001 through 010

**Files:**
- Create: `.tmp/evaluation/policy_0809_cuda_seeded2048_agent_choice_v3_kaggle_fp16_storage_fp32_runtime_v1/`
- Create: `docs/evaluation/combat_mat/policy_0809/0809_kaggle_top100_plus_v1_cuda_seeded_2048_agent_choice_v3_kaggle_fp16_storage_fp32_runtime_v1/`

**Interfaces:**
- Consumes: Task 2 CLI with `--deck-number 001` through `--deck-number 010`.
- Produces: ten independently valid Frozen-0809 CUDA-2048 reports and one aggregate index.

- [x] Run a small CUDA smoke on deck 001 and require both identity audits `PASS`, zero errors, complete Agent seat metadata, and strict FP32 runtime.
- [x] Run deck numbers 001 through 010 using the canonical seed and 256 resident lanes.
- [x] Validate 20,480 scheduled games, 20,480 terminal outcomes, zero errors, zero unfinished, exact per-game seed provenance, and ten deployment-effective hashes tied to the portable artifacts.
- [x] Open the aggregate and representative per-deck report data and cross-check W-L-D totals, seat totals, hashes, and report links.
- [x] Record the final report paths and results without making any automatic promotion decision.

### Task 4: Combat-mat UI and 14-axis aggregation

**Files:**
- Create: `evaluation/meta_archetypes_v1.json`
- Modify: `engine_cuda/tools/evaluate_policy_0809_cuda.py`
- Test: `engine_cuda/tests/test_policy_0809_cuda_evaluation.py`

**Interfaces:**
- Consumes: the immutable 55-deck catalog, representative-card metadata, completed 001-010 summaries, and the shared 14-axis taxonomy.
- Produces: a reference-compatible 001-055 table with card art and explicit pending rows, plus a 14-row weighted focal-policy win-rate aggregation and a separate Other evidence row.

- [x] Freeze the 14 ordered trigger-card classes as shared evaluation data with provenance.
- [x] Render all 55 numbered decks using the reference combat-mat columns and visual language.
- [x] Preserve report links only for completed decks and label 011-055 as untested without zero-valued pseudo-results.
- [x] Aggregate completed focal deck results by the 14-axis classifier and preserve an explicit Other bucket.
- [x] Test 55-row coverage, 14-class coverage, pending semantics, representative-card images, and W-L-D conservation.

### Task 5: Fixed two-level Combat Mat publication contract

**Files:**
- Create: `evaluation/combat_mat_contract.py`
- Modify: `evaluation/reporting/html.py`
- Modify: `engine_cuda/tools/evaluate_policy_0809_cuda.py`
- Modify: `docs/evaluation/combat_mat/README.md`
- Test: `engine_cuda/tests/test_policy_0809_cuda_evaluation.py`

- [x] Make the index contract require 001-055 presentation, card art, tested/pending semantics, 14 meta rows, and a visible Other row.
- [x] Make every completed detail report require exact-60 card art, 55 opponent rows, 14 opponent-meta rows, and an equivalent Other win-rate bar.
- [x] Aggregate opponent and opponent-meta W-L-D from the same per-game evidence and enforce conservation.
- [x] Reuse the established Frozen report visual language instead of a CUDA-specific simplified page.
- [x] Regenerate reports 001-010 from cached official-engine evidence and validate the complete publication.
