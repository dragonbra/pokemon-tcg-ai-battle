# 0016 R15 Gradual Option Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Train a fair 0016 R15 comparison that changes only the option ScaleGate initialization from 1.0 to 0.35 while preserving the completed V1 data and training contract.

**Architecture:** Freeze the 0016 source-conditioned R2 implementation as V1. Add project-local R15 modules that inherit the same R2 backbone, override only the final option-gate bias, and retain the source-persona residual. Allocate the formal run as `V2_win_multideck_r15_gradual_option` with the same dataset, seed, optimizer, batch sizes, validation cadence, early stopping and W&B project as V1.

**Tech Stack:** Python 3.11, PyTorch CUDA BF16, unittest, canonical JSONL/TensorBoard/W&B logging.

## Global Constraints

- Do not import executable code from `train/0014_faithful_board_causal_features` at runtime.
- Do not modify `engine/source/`.
- Keep `max_action_steps=64`, legal masks, source IDs and dataset commitment unchanged.
- Create a new strictly increasing repository version; never append to V1.
- Synchronize both `experiments/0016_alakazam_multideck_bc/DESIGN.md` and `DESIGN.html`.
- Do not create a git commit unless the user explicitly requests it.

---

### Task 1: Project-local R15 model contract

**Files:**
- Create: `train/0016_alakazam_multideck_bc/r15_model.py`
- Create: `train/0016_alakazam_multideck_bc/source_r15_model.py`
- Modify: `train/0016_alakazam_multideck_bc/tests/test_model_contract.py`

**Interfaces:**
- Consumes: `R2StrongScenarioPolicy`, `SourceModelConfig`, and the existing batch dictionary.
- Produces: `R15ModelConfig`, `R15DeterministicGradualOptionPolicy`, and `SourceConditionedR15Policy`.

- [ ] Add a failing test that asserts option-gate initial scale 0.35, state-gate scale 1.0, source-gate scale 0.05, `max_action_steps=64`, and parameter count 17,601,282.
- [ ] Run `python3 -m unittest -v train.0016_alakazam_multideck_bc.tests.test_model_contract` and verify the new imports fail.
- [ ] Implement the two local model modules with no cross-project imports.
- [ ] Re-run the model contract test and require all assertions to pass.

### Task 2: R15 runtime and formal training entrypoint

**Files:**
- Create: `train/0016_alakazam_multideck_bc/source_r15_inference.py`
- Create: `train/0016_alakazam_multideck_bc/run_r15.py`
- Create: `train/0016_alakazam_multideck_bc/export_r15_candidate.py`
- Modify: `train/0016_alakazam_multideck_bc/tests/test_project_contract.py`

**Interfaces:**
- Consumes: the existing materialized dataset, trainer, checkpoint schema, online encoder and canonical source vocabulary.
- Produces: a resumable formal V2 run and self-contained R15 candidate export.

- [ ] Add contract checks that R15 files stay inside project 0016 and contain no numbered-project imports.
- [ ] Implement checkpoint loading with schema `0016_source_r15_training_v1` and capacity-independent legal fallback.
- [ ] Implement the training entrypoint with V1-equivalent hyperparameters and W&B tags identifying R15.
- [ ] Implement candidate export with schema validation and physical runtime copies.
- [ ] Run compileall and the complete 0016 unit-test package.

### Task 3: Design synchronization and launch gate

**Files:**
- Modify: `experiments/0016_alakazam_multideck_bc/DESIGN.md`
- Modify: `experiments/0016_alakazam_multideck_bc/DESIGN.html`
- Create at runtime: `rl_runs/0016_alakazam_multideck_bc/versions/V2_win_multideck_r15_gradual_option/`

**Interfaces:**
- Consumes: verified model metadata and the frozen V1 training configuration.
- Produces: authoritative documentation and an online formal training run.

- [ ] Document the exact R2-to-R15 single-variable change, unchanged tensors/action contract, parameter count, objective and V2 gate.
- [ ] Run a fresh noncanonical CUDA smoke with 20 train and 4 validation batches.
- [ ] Compare smoke metadata against V1 for dataset hash, batch sizes, optimizer values, seed, source vocabulary and 64-step horizon.
- [ ] Launch `python3 -m train.0016_alakazam_multideck_bc.run_r15 --version V2_win_multideck_r15_gradual_option` and confirm status, GPU activity, first batches, TensorBoard files and stable W&B run ID.

### Task 4: TUFA evaluation after training launch

**Files:**
- Create under `.tmp/evaluation/`: a standardized TUFA runtime copy and temporary catalog.
- Create under `experiments/0016_alakazam_multideck_bc/evidence/deck_evaluations/`: three immutable reports and a manifest.

**Interfaces:**
- Consumes: the three existing epoch-12 candidates and `agent_bc_marnie_TUFA_deck` policy/deck.
- Produces: 100-game reports for Ito original, Ito plus two Battle Cage, and the complete submission deck.

- [ ] Replace only the temporary TUFA package's stale `cg/` copy with the current official evaluation runtime and validate policy/deck compatibility.
- [ ] Build a temporary catalog without admitting TUFA to the formal opponent pool.
- [ ] Run each epoch-12 candidate for 100 games with 50/50 seats, eight workers and one CPU thread per worker.
- [ ] Archive reports, hashes, commands, normalization note and summary metrics.
