# 0045 V8 U282 Eval-Every-Two Continuation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Continue the stopped 0045 V7 deck-007 specialist from exact U282 as a new V8 run, retaining the aggressive Policy-0809 rollout distribution while changing three-pool evaluation cadence from every update to every two updates.

**Architecture:** Add one version-specific V8 launch entry without changing shared PPO/model code. V8 strict-loads V7 U282 model-only weights, creates a fresh optimizer, recollects on-policy data, preserves Frozen-0045-Init as reference, and runs a same-contract U282 three-pool baseline before rollout. Thereafter synchronous three-pool CUDA-512 evaluation is due at even checkpoints U284, U286, U288, and so on.

**Tech Stack:** Python 3.11, PyTorch PPO, CUDA Engine 2.0, official engine runtime, pytest, TensorBoard, W&B online.

## Global Constraints

- Do not append to or mutate the stopped V7 training curve.
- V8 parent is exact V7 `update-000282.pt`, SHA-256 `25f312fa1d2c41a7ddbec2e3d4631c0437013781ed797a0d98b0c9f635d32d5c`.
- Optimizer and on-policy rollout data are fresh; no optimizer/RNG state is restored.
- Frozen-0045-Init remains the immutable specialist reference at update 0.
- Rollout remains exactly 512 games against complete immutable Policy-0809: Meta 03=200, 05=200, 02=50, other active Meta classes total=62.
- Model architecture, LR profile, entropy coefficient, PPO epochs/objective, reward shaping, and rollout size remain unchanged.
- The existing three disjoint Policy-0809 CUDA-512 pools remain unchanged.
- Evaluate the V8 U282 baseline before rollout; thereafter evaluate every two checkpoint updates at U284, U286, U288, and so on.
- Checkpoint retention remains `all`; no deletion is authorized.
- W&B uses a new stable V8 run ID in private project `dragon_bra/pokemon-tcg-policy-learning`.

---

### Task 1: V8 continuation contract

**Files:**
- Create: `train/0045_single_deck_expert_minimal_lora/training/run_v8_u282_aggressive_meta_quota_eval2.py`
- Test: `train/0045_single_deck_expert_minimal_lora/tests/test_aggressive_meta_quota.py`

**Interfaces:**
- Consumes: V7 U282 checkpoint, Frozen-0045-Init, existing exact-quota scheduler and cold-start LR profile.
- Produces: `readiness() -> dict[str, object]` and `launch(wandb_mode="online", updates=None) -> None`.

- [ ] Add a contract test that imports the V8 module and requires start/baseline update 282, parent V7, interval 2, Policy-0809-only rollout, exact quotas `{2: 50, 3: 200, 5: 200}`, unchanged LR profile, and unlimited updates.
- [ ] Run `python3 -m pytest train/0045_single_deck_expert_minimal_lora/tests/test_aggressive_meta_quota.py -q` and require the test to fail before the module exists.
- [ ] Implement the V8 entry by copying the audited V7 version shell and replacing only version identity, parent U282 identity, baseline update, evaluation interval, and W&B identity.
- [ ] Make readiness fail closed on used V8 paths, parent/reference hash mismatch, wrong model-only schema/update/version, or an incorrect first rollout schedule.
- [ ] Rerun the focused test and require PASS.

### Task 2: Cadence regression

**Files:**
- Test: `train/0045_single_deck_expert_minimal_lora/tests/test_aggressive_meta_quota.py`

**Interfaces:**
- Consumes: `periodic_evaluation.is_due(checkpoint_update, interval_updates=2)`.
- Produces: a regression assertion for V8 evaluation boundaries.

- [ ] Add assertions that U283 is not due, U284 is due, U285 is not due, and U286 is due.
- [ ] Run the focused test and require all cadence assertions PASS.

### Task 3: Authoritative project record

**Files:**
- Modify: `experiments/0045_single_deck_expert_minimal_lora/DESIGN.md`
- Modify: `experiments/0045_single_deck_expert_minimal_lora/DESIGN.html`
- Modify: `experiments/0045_single_deck_expert_minimal_lora/decisions.md`

**Interfaces:**
- Consumes: runtime-tested V8 constants and the stopped V7/U282 evidence boundary.
- Produces: synchronized human-readable architecture/training state.

- [ ] Record that V7 stopped after durable U282, with U281 as its final complete three-pool point and U282 as a separately complete Benchmark V2 CUDA-2048 point.
- [ ] Document V8 parent U282, fresh optimizer/on-policy data, unchanged aggressive quotas/opponent/reference, and the U282 baseline plus even-update evaluation cadence.
- [ ] Cross-check both DESIGN formats against the V8 readiness payload.

### Task 4: Verification and launch

**Files:**
- Generate: `rl_runs/0045_single_deck_expert_minimal_lora/versions/V8_dragapult_007_u282_aggressive_meta_quota_policy0809_eval2/`

**Interfaces:**
- Consumes: tested V8 entry point and all immutable identities.
- Produces: an unlimited formal V8 W&B run.

- [ ] Run the focused test and the complete 0045 test suite.
- [ ] Run V8 readiness and verify unused output paths, U282/Frozen-0045-Init hashes, exact first schedule, interval 2, and W&B online identity.
- [ ] Launch V8 without an update limit.
- [ ] Verify the U282 three-pool baseline completes before the first U282-source rollout.
- [ ] Verify U283 writes canonical metrics without periodic eval, then U284 triggers the next three-pool evaluation before U284-source rollout begins.
