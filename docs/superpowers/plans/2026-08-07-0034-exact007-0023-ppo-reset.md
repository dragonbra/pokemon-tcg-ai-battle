# 0034 Exact 007 / 0023 PPO Reset Implementation Plan

> **For agentic workers:** Execute this plan inline and track every test gate. Do not commit unless the user explicitly requests it.

**Goal:** Start a fresh 0034 decoder-only PPO run from Policy-0806 with exact Frozen deck 007 and the audited 0023 temporal-credit/PPO settings.

**Architecture:** Keep the 0034 self-contained full-semantic actor, official engine rollout workers, immutable Policy-0806 opponents, fixed 256-game Frozen-0806 schedule, and termination guard. Change only the focal exact deck and the PPO credit/update settings inherited from 0023; make checkpoint probes use a checkpoint-independent seed schedule and record an update-0 baseline so subsequent strength curves are comparable.

**Tech Stack:** Python 3.11, PyTorch PPO, official CPU engine workers, shared CUDA inference, unittest, W&B online logging.

## Global Constraints

- Focal deck is Frozen-0806 deck number `007`, exact deck SHA-256 `07bedfffbfad6ecb31733acc54c8110bb1934d8b1dc98bd9c4d37f6ba5c5e725`.
- Initialization and all frozen opponents use `archive/pretrained/0031_friend_0806_epoch11_best_validation_loss/model.pt`.
- Every PPO rollout uses the fixed 256-game `0806_kaggle_top100_plus_v1` schedule.
- Preserve the repeated-action opponent-win rule and 50-full-round draw rule.
- Restore audited 0023 PPO settings: selection clock, `gae_lambda=0.95`, `gamma=1.0`, PPO epochs `4`, minibatch `1024`; preserve the other matching coefficients.
- Freeze the representation and train only `actor.action_decoder.*` plus `value_head.*`.
- Retain every model-only checkpoint and create a fresh monotonic version.
- Do not edit `engine/source/` and do not commit without explicit user authorization.

---

### Task 1: Contract Tests

**Files:**
- Modify: `train/0034_dragapult_third_large_model_rl/tests/test_project_identity.py`
- Modify: `train/0034_dragapult_third_large_model_rl/tests/test_full_terminal_credit.py`

**Interfaces:**
- Consumes: `run_full_semantic.focal_deck()`, `build_jobs()`, `RunConfig`, and `PPOConfig`.
- Produces: regression gates for exact deck identity, 0023 settings, and fixed probe seeds.

- [ ] Assert that `focal_deck()` is the same 60-card multiset as the Frozen catalog entry `dragapult_ex_07bedfffbfad`.
- [ ] Assert that the formal `RunConfig` uses selection clock, lambda `0.95`, batch `1024`, and four PPO epochs.
- [ ] Assert that greedy jobs for different checkpoint updates receive identical seeds, seats, opponents and decks, while sampled rollout jobs retain update-dependent seeds.
- [ ] Run the focused tests and confirm they fail before implementation.

### Task 2: Exact 007 and Comparable Probes

**Files:**
- Modify: `train/0034_dragapult_third_large_model_rl/training/run_full_semantic.py`

**Interfaces:**
- Consumes: the existing 55-deck Frozen catalog and exact 007 deck asset.
- Produces: exact 007 rollouts and checkpoint-independent greedy evaluation jobs.

- [ ] Point `FOCAL_DECK_PATH` at `league/decks/dragapult_ex_07bedfffbfad/deck.csv` and record its deck ID/hash in training config and W&B tags.
- [ ] Make `build_jobs(..., greedy=True)` omit `source_policy_update` from the engine seed while preserving update-dependent sampled rollout seeds.
- [ ] Run a 256-game greedy baseline for checkpoint update 0 and log it at step 0 before the first PPO rollout.
- [ ] Keep update probes at every ten updates on the exact same 256 seeds, seats, decks and order as update 0.

### Task 3: Restore 0023 PPO Semantics

**Files:**
- Modify: `train/0034_dragapult_third_large_model_rl/training/run_full_semantic.py`

**Interfaces:**
- Consumes: `PPOConfig` and `prepare_episodes` selection-clock support already frozen inside 0034.
- Produces: a fresh V6 configuration matching 0023 credit/update semantics while retaining the current Frozen environment.

- [ ] Set formal defaults to selection clock and lambda `0.95`.
- [ ] Set PPO defaults used by the formal runner to epochs `4` and batch size `1024`.
- [ ] Preserve actor/value learning rates, clip ratio, entropy, reference KL, target behavior KL, gamma, loss weighting, and gradient clipping.
- [ ] Record the 0023 source version/config path and intentional 256-game deviation in `training_config.json`.

### Task 4: Documentation and Verification

**Files:**
- Modify: `experiments/0034_dragapult_third_large_model_rl/DESIGN.md`
- Modify: `experiments/0034_dragapult_third_large_model_rl/DESIGN.html`
- Modify: `experiments/0034_dragapult_third_large_model_rl/DECISIONS.md`
- Modify: `experiments/0034_dragapult_third_large_model_rl/manifest.json`

**Interfaces:**
- Consumes: verified code constants, exact deck manifest, and run configuration.
- Produces: authoritative documentation matching the new focal deck and PPO contract.

- [ ] Document V5 as stopped after observed greedy regression without deleting its artifacts.
- [ ] Document exact 007, selection-clock lambda `0.95`, four epochs, batch `1024`, fixed-seed update-0/periodic probes, and the unchanged official-engine termination rules.
- [ ] Run focused tests, all 0034 tests, and a small official-engine PPO gate.
- [ ] Launch fresh `V6_exact007_0023_selection_lambda095` for 500 updates with 32 workers, 256 games/update, evaluation every ten updates, and W&B online.
- [ ] Verify the process, GPU allocation, update-0 baseline, W&B run, zero engine errors, and first model-only checkpoint.
