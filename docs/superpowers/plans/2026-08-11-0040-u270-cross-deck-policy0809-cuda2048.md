# 0040 U270 Cross-Deck Policy-0809 CUDA-2048 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Execute this plan inline in the current session. Subagent execution is disabled by the repository session contract. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Evaluate the immutable 0040 update-270 compound policy with exact focal decks 007, 002, 009, and 003 against the complete independent Policy-0809 Frozen pool for 2,048 official CUDA games per focal deck.

**Architecture:** Generalize the project-local 0040 exporter so exact-deck identity is an explicit input while U270 effective weights remain unchanged. A self-contained 0040 driver will materialize and audit one FP16-storage/FP32-runtime package per focal deck, resolve a full independent Policy-0809 opponent, build eight immutable 256-slot schedules, run the existing compound CUDA resident collector, and publish both a two-level Combat Mat and a formal V4 experiment summary.

**Tech Stack:** Python 3.11, PyTorch CUDA, official CUDA engine, 0040 compound-action runtime, Policy Identity V1, JSON/HTML reporting, unittest.

## Global Constraints

- Source checkpoint is exactly `rl_runs/0040_dragapult_0809_action_boundary_rl/versions/V2_snapshot_loader_fix_long_run/checkpoint/update-000270.pt`, SHA-256 `c87bcf82ca2b22eed65876da7d0ec0648a85d72d40dd13dd614290e7de002142`.
- Focal weights are one immutable U270 effective policy; only exact-deck conditioning changes across 007, 002, 009, and 003.
- Opponent is independently resolved complete `Policy-0809`; candidate materialization must never mutate or share candidate weights into the opponent.
- Candidate contract is `kaggle_fp16_storage_fp32_runtime_v1`; every package must persist source, portable, and deployment-effective hashes with `PASS` status.
- Evaluation seed is `341512806`; each focal deck uses replicas `0..7`, the exact 256-slot pool frequency, seeded toss, and Agent-owned context-41 first-player choice.
- Every focal deck must finish 2,048 terminal official-engine games with zero errors, zero unfinished games, and zero semantic fallback.
- Do not modify `engine/source/` and do not overwrite any existing 0040 version or Combat Mat report.
- No automatic promotion decision is permitted.

---

### Task 1: Exact-deck U270 candidate materialization

**Files:**
- Modify: `train/0040_dragapult_0809_action_boundary_rl/export_full_semantic_candidate.py`
- Modify: `train/0040_dragapult_0809_action_boundary_rl/candidate_deployment.py`
- Test: `train/0040_dragapult_0809_action_boundary_rl/tests/test_u270_policy0809_cuda.py`

**Interfaces:**
- Consumes: U270 checkpoint, audited 0809 source package, explicit deck path, deck ID, and display name.
- Produces: `export_candidate(..., deck_path, deck_id, deck_display_name)` and a persistent strict-loaded FP32 runtime whose audit binds the U270 source and portable hashes.

- [x] Write a failing test proving export currently rejects a non-007 exact deck.
- [x] Add explicit exact-deck arguments while retaining 007 as the backward-compatible default.
- [x] Validate exact 60 cards, official catalog membership, deck file SHA-256, sorted-deck identity SHA-256, and manifest deck provenance.
- [x] Confirm the four packages have identical deployment-effective weight hashes and distinct exact-deck hashes.
- [x] Run the focused exporter and candidate-deployment tests.

### Task 2: Formal Policy-0809 CUDA driver

**Files:**
- Create: `train/0040_dragapult_0809_action_boundary_rl/evaluate_u270_policy0809_cuda.py`
- Test: `train/0040_dragapult_0809_action_boundary_rl/tests/test_u270_policy0809_cuda.py`

**Interfaces:**
- Consumes: focal deck numbers `007,002,009,003`, the Frozen 55-deck catalog, U270 packages, and independently materialized Policy-0809.
- Produces: resumable schedule/result JSON with complete per-game seed, toss, Agent choice, actual seat, terminal, fallback, policy identity, and deployment identity evidence.

- [x] Write schedule tests for exactly 2,048 unique jobs, eight 256-slot replicas, all 55 opponents, and both requested policy identities.
- [x] Implement fail-closed U270 checkpoint/sidecar/package preflight and full Policy-0809 opponent audit before CUDA engine construction.
- [x] Strict-load each portable package to FP32 and pass its compound actor/value/allocation/meta modules to the existing 0040 CUDA collector.
- [x] Run a small 007 CUDA smoke requiring both audits `PASS`, complete context-41 metadata, and zero semantic fallback.
- [x] Add resumable cache validation that rejects any schedule, deck, checkpoint, portable, effective, opponent, engine, or result mismatch.

### Task 3: Four formal CUDA-2048 runs

**Files:**
- Create: `.tmp/evaluation/0040_u270_policy0809_cuda2048/`

**Interfaces:**
- Consumes: Task 2 driver and exact focal decks in order 007, 002, 009, 003.
- Produces: 8,192 terminal outcomes and four independently auditable cached result files.

- [x] Run 007 for 2,048 games and validate zero error/unfinished/semantic fallback.
- [x] Run 002 for 2,048 games and validate zero error/unfinished/semantic fallback.
- [x] Run 009 for 2,048 games and validate zero error/unfinished/semantic fallback.
- [x] Run 003 for 2,048 games and validate zero error/unfinished/semantic fallback.
- [x] Cross-check per-deck W-L-D, actual first/second splits, all 55 opponent aggregates, 14 meta rows plus Other, and hash conservation.

### Task 4: Durable reports and formal V4 record

**Files:**
- Create: `docs/evaluation/combat_mat/policy_0809/u270_cross_deck_002_003_007_009_vs_frozen_0809_cuda_seeded_2048_agent_choice_v3_kaggle_fp16_storage_fp32_runtime_v1/`
- Create: `experiments/0040_dragapult_0809_action_boundary_rl/evaluation/V4_u270_cross_deck_policy0809_cuda2048.html`
- Modify: `experiments/0040_dragapult_0809_action_boundary_rl/evaluation/index.html`
- Create: `rl_runs/0040_dragapult_0809_action_boundary_rl/versions/V4_u270_cross_deck_policy0809_cuda2048/artifact/evaluation.json`
- Create: `rl_runs/0040_dragapult_0809_action_boundary_rl/versions/V4_u270_cross_deck_policy0809_cuda2048/artifact/status.json`

**Interfaces:**
- Consumes: the four validated result files and the shared `evaluation.combat_mat_contract` renderer contract.
- Produces: a 55-row main view and four full detail pages with exact-60 card art, 55 opponent bars, 14 meta bars plus Other, plus a formal project-level comparison page.

- [x] Refuse to publish if the V4 or Combat Mat destination already exists.
- [x] Render the shared two-level Combat Mat without simplifying the established UI.
- [x] Render the V4 summary with all four records, overall totals, source/portable/effective hashes, opponent identity, and explicit no-promotion statement.
- [x] Validate 8,192 games, zero error/unfinished/fallback, four detail pages, 55 opponent rows, 14 meta rows plus Other per detail, and working report links.
- [x] Run focused and shared reporting tests, `py_compile`, and `git diff --check`.
