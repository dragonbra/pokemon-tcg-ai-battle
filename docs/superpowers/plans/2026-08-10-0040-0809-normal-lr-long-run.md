# 0040 0809 Normal-LR Long-Run Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Start a self-contained 0040 Dragapult PPO project from the paired 0809 Zero-Shot policy and Value checkpoints, using the proven 0038 Action Boundary stack at normal learning rates and no update limit.

**Architecture:** Freeze the repaired 0038 Action Boundary/CUDA rollout implementation into a new numbered project without runtime imports from 0038. Build U0 from the strict 0809 actor and paired 0809 Value head, overlay only the previously BC-trained allocation head, initialize Last Option Q/V LoRA at zero delta, and retain the PRIZE preset and 512-game fixed-optimizer-budget PPO contract. CUDA Frozen evaluation uses the v3 2,048-game fixed-seed schedule and lets the toss winner's Agent choose first or second through context 41.

**Tech Stack:** Python 3.11, PyTorch, custom PPO, CUDA resident official-rule clone, official observation compiler, W&B online logging.

## Global Constraints

- Project ID is `0040_dragapult_0809_action_boundary_rl`; it must not import executable code from another numbered `train/` project.
- Focal exact deck is Frozen-0806 slot 007, `dragapult_ex_07bedfffbfad`.
- Actor source is `archive/pretrained/0031_friend_0809_gsb_v5_value_v9/model.pt`, SHA-256 `926321955b6f3144b62e65899b5041ca3a47b17f305202ba9dffc0c92aaa7c7f`.
- Value source is `archive/pretrained/0031_friend_0809_gsb_v5_value_v9/value_head.pt`, SHA-256 `f8cb92f45f625e6519b6f103deb6d4ef68323fca93037ebd4c25db9da122a486`.
- Do not load any 0038 PPO-updated actor, Value, LoRA, optimizer, scheduler, rollout, replay, old-logprob, or GAE state.
- The only reusable learned 0038 sidecar is the pre-PPO allocation BC head from the attested U0 checkpoint.
- Use normal base rates: Action Decoder/Allocation `1e-5`, Last Option Q/V LoRA `3e-5`, Value/Prize `1e-4`; do not enable accelerated-transfer multipliers.
- Use 512 rollout games, 512 trajectories, 32 optimizer steps, fixed optimizer budget, four PPO epochs, evaluation every five updates, and no maximum update count.
- Do not modify `engine/source/`.
- W&B must be online in `dragon_bra/pokemon-tcg-policy-learning` with a new stable 0040 run ID.

---

### Task 1: Freeze a self-contained 0040 source tree

**Files:**
- Create: `train/0040_dragapult_0809_action_boundary_rl/`
- Create: `experiments/0040_dragapult_0809_action_boundary_rl/DESIGN.md`
- Create: `experiments/0040_dragapult_0809_action_boundary_rl/DESIGN.html`

**Interfaces:**
- Consumes: committed 0038 source at `b35f206`.
- Produces: importable `train.0040_dragapult_0809_action_boundary_rl` with no `train.0038_*` runtime dependency.

- [ ] Copy the 0038 implementation while excluding caches and generated run state.
- [ ] Rewrite Python package imports and project-output roots to 0040.
- [ ] Add a project-identity test that scans Python runtime files and rejects executable imports from another numbered project.
- [ ] Run import/compile tests and require PASS.

### Task 2: Implement strict paired-0809 U0 initialization

**Files:**
- Modify: `train/0040_dragapult_0809_action_boundary_rl/source.py`
- Modify: `train/0040_dragapult_0809_action_boundary_rl/initialization.py`
- Modify: `train/0040_dragapult_0809_action_boundary_rl/policy/actor_critic.py`
- Modify: `train/0040_dragapult_0809_action_boundary_rl/checkpoint.py`
- Test: `train/0040_dragapult_0809_action_boundary_rl/tests/test_0040_initialization.py`

**Interfaces:**
- Consumes: strict 0809 policy/Value checkpoints and the 0038 pre-PPO allocation-head tensors.
- Produces: `build_preset_from_common_update0(deck, flags, device)` whose actor decoder and Value tensors come from 0809, LoRA is zero delta, and allocation tensors equal the BC source.

- [ ] Write tests for exact policy/value hashes, strict state inventories, Value source-policy linkage, zero-delta LoRA, and allocation-only overlay.
- [ ] Replace 0806 source constants with the paired 0809 paths and hashes.
- [ ] Change common-U0 overlay to accept only `allocation_head.*`; reject actor/value/auxiliary overlays.
- [ ] Save a new 0040 model-only U0 and verify it contains no optimizer or rollout state.

### Task 3: Lock normal PPO and Frozen-v3 contracts

**Files:**
- Modify: `train/0040_dragapult_0809_action_boundary_rl/training/run_full_semantic.py`
- Modify: `train/0040_dragapult_0809_action_boundary_rl/training/storage_full_semantic.py`
- Test: `train/0040_dragapult_0809_action_boundary_rl/tests/test_0040_long_run_contract.py`

**Interfaces:**
- Consumes: the paired-0809 U0 model.
- Produces: `RunConfig(version="V1_0809_normal_lr_long_run", updates=None, preset_name="PRIZE")` and an infinite manual-stop training loop.

- [ ] Assert optimizer groups use exactly `1e-5`, `3e-5`, `1e-4`, and `1e-4` for Actor, LoRA, Value, and Prize respectively.
- [ ] Assert accelerated-transfer mode and LR multiplier controller are disabled.
- [ ] Assert rollout/evaluation sizes are 512/2,048 and evaluation interval is five.
- [ ] Assert Frozen jobs use `frozen_0806_seeded_agent_first_player_v3`, evaluation seed `341512806`, and context-41 Agent choice.
- [ ] Assert `updates=None`, all model-only checkpoints are retained, and `STOP_REQUESTED` is checked only at complete-update boundaries.

### Task 4: Synchronize design and provenance

**Files:**
- Create: `experiments/0040_dragapult_0809_action_boundary_rl/manifest.json`
- Create: `experiments/0040_dragapult_0809_action_boundary_rl/DECISIONS.md`
- Modify: `experiments/0040_dragapult_0809_action_boundary_rl/DESIGN.md`
- Modify: `experiments/0040_dragapult_0809_action_boundary_rl/DESIGN.html`

**Interfaces:**
- Consumes: verified implementation constants and source hashes.
- Produces: authoritative 0040 model/action/loss/rollout documentation.

- [ ] Record the 0809 actor and Value provenance, allocation-only BC reuse, normal optimizer groups, PRIZE preset, Frozen-v3 evaluation, and indefinite run lifecycle.
- [ ] Verify DESIGN Markdown and HTML agree on all checkpoint hashes, rates, feature/action schemas, and current project phase.

### Task 5: Test, commit, and launch V1

**Files:**
- Create at runtime: `rl_runs/0040_dragapult_0809_action_boundary_rl/versions/V1_0809_normal_lr_long_run/`

**Interfaces:**
- Consumes: Tasks 1–4.
- Produces: committed source plus a running W&B-backed formal training process.

- [ ] Run focused initialization, Action Boundary, CUDA rollout, PPO, storage, Frozen-panel, and export tests.
- [ ] Require clean Git source identity, then commit 0040 implementation and current completed evaluation assets.
- [ ] Launch without `--updates`, with `--launch-formal --preset PRIZE --engine-backend accelerated:cuda_resident --eval-every 5`.
- [ ] Verify U0 strict checkpoint creation, W&B online status, and U0 Frozen-2048 completion.

### Task 6: Guard through update 5 acceptance

**Files:**
- Verify: `rl_runs/0040_dragapult_0809_action_boundary_rl/versions/V1_0809_normal_lr_long_run/artifact/training_metrics.jsonl`
- Verify: `rl_runs/0040_dragapult_0809_action_boundary_rl/versions/V1_0809_normal_lr_long_run/checkpoint/update-000005.pt`
- Verify: `rl_runs/0040_dragapult_0809_action_boundary_rl/versions/V1_0809_normal_lr_long_run/artifact/frozen_results/core-update-000005.json`

**Interfaces:**
- Consumes: live V1 process.
- Produces: first accepted long-run checkpoint/evaluation boundary while leaving training active.

- [ ] Monitor every completed update for NaN/Inf, runtime error, invalid macro, unsupported effect, fallback, pending reset, KL/clip, Value loss/EV, memory, and throughput.
- [ ] If a deterministic implementation bug occurs, stop at the update boundary, preserve evidence, patch and test, allocate a fresh V2 unless exact resume semantics remain valid, then restart.
- [ ] Require update 5 model-only checkpoint and SHA sidecar.
- [ ] Require Frozen U5 to contain 2,048 terminal games, fixed seed/schedule, Agent-selected seats, zero runtime errors, and persisted aggregate/per-game evidence.
- [ ] Leave the healthy process running with no update cap and report PID, W&B URL, U0/U5 results, health metrics, and checkpoint paths.
