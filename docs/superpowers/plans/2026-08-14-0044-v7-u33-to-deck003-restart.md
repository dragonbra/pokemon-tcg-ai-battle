# 0044 V7/V8 U33-to-Deck003 Restart Implementation Plan

> V7 failed closed before checkpoint, Benchmark, rollout, or W&B creation because
> the legacy model loader hardcoded exact-deck identity `007`. The implementation
> continues as V8 after adding explicit deck-ID resolution; all experiment semantics
> below are unchanged except the required monotonically increasing version/run ID.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Continue the latest durable 0044 V6 U33 policy as a new Deck-003 focal PPO version whose local update axis starts at U0 and whose first strength point is a Core-16 Benchmark V2 CUDA-2048 U0 evaluation.

**Architecture:** Preserve the V6 model, optimizer hyperparameters, immutable Champion-G2 rollout opponent, uniform `001`–`067` opponent deck sampling, CUDA Engine 2.0, and no-PFSP semantics. Load V6 U33 model-only weights into a fresh optimizer, bind own-deck conditioning and focal deck bytes to exact deck `003`, save the inherited weights as the new version's U0, run Benchmark V2 before collecting any rollout, then continue locally as U1, U2, and later updates. Record both the local update and source V6 U33 provenance so the reset cannot be mistaken for a fresh Champion-G2 initialization.

**Tech Stack:** Python 3.11, PyTorch PPO, CUDA Engine 2.0, official engine runtime, W&B, pytest.

## Global Constraints

- Parent checkpoint is exactly V6 `update-000033.pt`, SHA-256 `a7bc5a488ad031c07be96576113762b40cb75c51d46370d1e7eace22df818cbf`.
- New repository version is V8 and uses a fresh W&B run ID; the used V7 directory is retained.
- New local checkpoint/update axis starts at U0; provenance records source version V6 and source update U33.
- Focal exact deck and own-deck conditioning are both `003`; opponent lanes remain immutable Champion-G2 with exact deck-conditioned rows.
- Opponent decks are sampled uniformly with replacement from exact IDs `001`–`067`; PFSP state is neither loaded nor written.
- Benchmark V2 runs before the first rollout, using Deck 003, complete immutable Policy-0809, Core-16 Meta IDs `00`–`13`, `17`, `27`, exactly 128 games per Meta, common random numbers, and `kaggle_fp16_storage_fp32_runtime_v1`.
- Training checkpoint storage remains model-only FP32 with retention `all`; evaluation materialization must not mutate PPO weights.
- Existing V1–V6 runtime artifacts and the newly published Deck-007 report remain immutable.

---

### Task 1: Generalize the focal-deck launch boundary

**Files:**
- Modify: `train/0044_g2_dragapult_policy_option_lora/training/run_v1.py`
- Test: `train/0044_g2_dragapult_policy_option_lora/tests/test_v7_deck003_restart.py`

**Interfaces:**
- Consumes: `run(..., focal_deck_ids=("003",), focal_deck_id="003")`.
- Produces: a model, checkpoint metadata, rollout jobs, own-archetype routing, and periodic evaluation all bound to the same exact focal deck.

- [ ] **Step 1: Write a failing test** asserting the training entrypoint no longer hardcodes deck `007` at model load, checkpoint metadata, or Benchmark V2 invocation.
- [ ] **Step 2: Run the focused test** with `python3 -m pytest -q train/0044_g2_dragapult_policy_option_lora/tests/test_v7_deck003_restart.py` and confirm failure before implementation.
- [ ] **Step 3: Add an explicit `focal_deck_id` argument** defaulting to `007`, validate that it is the singleton member of `focal_deck_ids` for fixed-focal runs, and use it for model loading, checkpoint metadata, and periodic evaluation.
- [ ] **Step 4: Run the focused test** and confirm PASS without changing V6 defaults.

### Task 2: Add the V8 reset-and-provenance entrypoint

**Files:**
- Create: `train/0044_g2_dragapult_policy_option_lora/training/run_v8.py`
- Test: `train/0044_g2_dragapult_policy_option_lora/tests/test_v8_deck003_restart.py`

**Interfaces:**
- Consumes: V6 U33 model-only checkpoint and the generalized `run()` entrypoint.
- Produces: `V8_u33_deck003_core16_benchmark_v2`, local U0, a fresh optimizer, no PFSP state, and stable W&B run ID `0044-v8-u33-deck003-core16-benchmark-v2`.

- [ ] **Step 1: Extend the test** to require exact parent path/hash/update, local start U0, Deck 003, uniform sampling, low learning rates, and Core-16 U0 baseline.
- [ ] **Step 2: Implement `readiness()`** with hard gates for parent identity, asset/CUDA readiness, unused version paths, and the declared configuration.
- [ ] **Step 3: Implement `main()`** so `--launch-formal` passes `start_update=0`, `parent_checkpoint=V6 U33`, `baseline_evaluation_checkpoint=0`, `focal_deck_id="003"`, and `focal_deck_ids=("003",)`.
- [ ] **Step 4: Run focused and complete 0044 tests** and require all tests to pass.

### Task 3: Synchronize authoritative design and launch

**Files:**
- Modify: `experiments/0044_g2_dragapult_policy_option_lora/DESIGN.md`
- Modify: `experiments/0044_g2_dragapult_policy_option_lora/DESIGN.html`
- Modify: `train/0044_g2_dragapult_policy_option_lora/README.md`
- Runtime create: `rl_runs/0044_g2_dragapult_policy_option_lora/versions/V8_u33_deck003_core16_benchmark_v2/`

**Interfaces:**
- Consumes: V8 readiness and implementation.
- Produces: authoritative Deck-003/U0 semantics, a PASS pre-rollout Benchmark V2 point, then continuing PPO checkpoints.

- [ ] **Step 1: Update both DESIGN formats and README** with Deck 003, source V6 U33, local-U0 reset, fresh optimizer, unchanged opponent/value/LoRA contracts, and Benchmark-before-rollout semantics.
- [ ] **Step 2: Run readiness** and save the diagnostic under `.tmp/evaluation/0044_v8_deck003_readiness/`.
- [ ] **Step 3: Launch V8 online** with `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` and no finite `--updates` limit.
- [ ] **Step 4: Verify U0 Benchmark V2** has 2,048 valid games, 128 per selected Meta, Policy-0809 identity PASS, zero routing failures, zero feature D2H, and is logged at local `eval/checkpoint_update=0` before `rollout/source_policy_update=0` appears.
- [ ] **Step 5: Verify the first PPO update** produces a durable model-only U1 checkpoint, 256 valid Champion-G2 uniform rollout games, finite PPO metrics, and online W&B synchronization.
