# 0043 U407 G2 Candidate Gate Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stop V6 exactly after atomic U407 completion and compare immutable Champion-G1 with the U407 G2 candidate across twenty focal decks under separate formal Frozen Policy-0809 CUDA-2048 evaluations.

**Architecture:** A strict stop gate requires both the U407 model-only checkpoint and canonical metric row before stopping V6. A restart-safe V7 evaluation controller freezes a seeded sample of ten decks from 030-067 plus all decks 001-010, materializes either Champion-G1 or U407 under `kaggle_fp16_storage_fp32_runtime_v1`, runs each arm serially through CUDA Engine 2.0, and publishes per-deck and aggregate deltas without registering Champion-G2 automatically.

**Tech Stack:** Python 3.11, PyTorch, CUDA Engine 2.0, official engine runtime, HTML/JSON, systemd user services.

## Global Constraints

- Stop only after checkpoint U407, its canonical metric row, and committed PFSP state exist; U408 must not become a completed checkpoint.
- Random sample seed is `430043407`; the immutable 030-067 sample is `031,039,040,042,047,056,057,058,061,064`, and this cohort executes before the remaining 001-010 arms.
- Fixed cohort is exactly `001,002,003,004,005,006,007,008,009,010`.
- Evaluate Champion-G1 and U407 G2 candidate separately for every focal deck against complete immutable Policy-0809.
- Every arm is greedy, official-engine CUDA-2048, candidate FP16 storage / FP32 runtime, 2,048 terminal games, zero error/unfinished, routing PASS, and feature D2H zero.
- Preserve candidate/opponent weight isolation and exact-deck conditioning; do not modify `engine/source/`.
- U407 remains `G2 candidate`; only a later explicit human `PROMOTE` decision may register Champion-G2.
- Do not overwrite prior evaluation versions or unrelated user worktree changes.

---

### Task 1: Freeze sampling and evaluation identity contracts

**Files:**
- Create: `train/0043_champion_league_rl/evaluation/g2_candidate_gate.py`
- Create: `train/0043_champion_league_rl/tests/test_g2_candidate_gate.py`

**Interfaces:**
- Consumes: Champion-G1 registry artifacts, V6 U407 checkpoint, 0043 deck registry and own-archetype mapping.
- Produces: `evaluation_decks() -> tuple[str, ...]`, Champion-G1 candidate materialization, strict arm validation, and a restart-safe controller CLI.

- [ ] **Step 1: Write deterministic cohort tests**

Assert seed `430043407` produces exactly the declared ten random decks, the fixed cohort is 001-010, the union contains twenty unique IDs, and every ID resolves to one immutable exact-60 asset.

- [ ] **Step 2: Write identity fail-closed tests**

Assert the controller rejects a missing/non-U407 checkpoint, non-PASS candidate/opponent identity, non-2048 game count, error/unfinished games, shared parameter storage, routing failures, or feature D2H.

- [ ] **Step 3: Implement Champion-G1 and U407 arm execution**

Champion-G1 must load its registered immutable FP16 portable artifact into FP32 runtime with the deck's declared parent G1 own-embedding row. U407 must use the existing 0043 candidate materializer with the exact 29-way own-deck row. Both arms independently materialize complete frozen Policy-0809 and run the registered eight-replica CUDA-2048 schedule.

- [ ] **Step 4: Run focused tests**

Run: `PYTHONPATH=. python3 -m pytest -q train/0043_champion_league_rl/tests/test_g2_candidate_gate.py train/0043_champion_league_rl/tests/test_evaluation_preflight.py train/0043_champion_league_rl/tests/test_frozen_schedule.py`

### Task 2: Execute the restart-safe 40-arm assessment

**Files:**
- Create at runtime: `rl_runs/0043_champion_league_rl/versions/V7_u407_g2_candidate_cross_deck_cuda2048/artifact/`
- Create at runtime: `rl_runs/0043_champion_league_rl/versions/V7_u407_g2_candidate_cross_deck_cuda2048/checkpoint/`

**Interfaces:**
- Consumes: immutable U407 and the tested controller.
- Produces: one state manifest plus forty immutable per-arm JSON reports.

- [ ] **Step 1: Validate the U407 terminal boundary**

Require V6 inactive, U407 strict-load PASS, no U408 checkpoint, canonical last metric at U407/source-U406, and PFSP observations committed through source-U406.

- [ ] **Step 2: Launch the controller as a persistent user service**

Run one arm at a time on the RTX 5080. After every arm, validate and atomically update state so a reboot resumes at the first missing arm rather than repeating valid evidence.

- [ ] **Step 3: Validate complete scope**

Require 20/20 decks and 40/40 arms, exactly 81,920 terminal games, zero error/unfinished, and identity/route/residency PASS for every arm.

### Task 3: Publish the G1 versus G2-candidate diagnostic

**Files:**
- Create: `docs/evaluation/combat_mat/policy_0809/u407_g2_candidate_vs_champion_g1_cuda_seeded_2048_agent_choice_v3_kaggle_fp16_storage_fp32_runtime_v1/index.html`
- Create: `docs/evaluation/combat_mat/policy_0809/u407_g2_candidate_vs_champion_g1_cuda_seeded_2048_agent_choice_v3_kaggle_fp16_storage_fp32_runtime_v1/reports/<deck_id>.html`
- Create: `docs/evaluation/combat_mat/policy_0809/u407_g2_candidate_vs_champion_g1_cuda_seeded_2048_agent_choice_v3_kaggle_fp16_storage_fp32_runtime_v1/manifest.json`
- Create: `rl_runs/0043_champion_league_rl/versions/V7_u407_g2_candidate_cross_deck_cuda2048/artifact/evaluation.json`
- Modify: `experiments/0043_champion_league_rl/DECISIONS.md`

**Interfaces:**
- Consumes: forty validated arm reports.
- Produces: one authoritative human-readable report and immutable reverse link.

- [ ] **Step 1: Aggregate paired deck-level deltas**

Report G1 and G2-candidate W-L-D, win rate, Wilson interval, actual first/second-player splits, delta in percentage points, improved/flat/regressed counts, aggregate micro and deck-macro deltas, best improvement, worst regression, and cohort-separated summaries. “Flat” is descriptive and must expose its declared threshold rather than imply statistical equivalence.

- [ ] **Step 2: Render the audit report**

Follow the Champion-G1 diagnostic information architecture: overview, cohort tables, exact deck identities, representative cards, per-arm deployment hashes, Policy-0809 identity, schedule identities, CUDA identity, evidence limitations, and explicit `HUMAN_DECISION_REQUIRED` status.

- [ ] **Step 3: Refresh the project evaluation index and decision record**

Link the V7 report, record that U407 is only a G2 candidate, and preserve the final operator choices `PROMOTE`, `HOLD`, or `REJECT` for the user.

- [ ] **Step 4: Run final report validation**

Verify all twenty rows and forty arm identities are present, links resolve, embedded JSON parses, the source checkpoint hash is uniform for every G2 arm, Champion-G1 hashes are immutable, and no result is labeled Champion-G2 before human promotion.

### Task 4: Continue to reverse-order Full-67 if no new instruction arrives

**Files:**
- Create: `train/0043_champion_league_rl/evaluation/g2_candidate_full67.py`
- Create: `train/0043_champion_league_rl/tests/test_g2_candidate_full67.py`
- Create at runtime: `rl_runs/0043_champion_league_rl/versions/V8_u407_g2_candidate_full67_cuda2048/`

- [ ] Wait for the V7 20-deck gate and its report to complete.
- [ ] Strictly inherit the 20 existing deck-arm reports without rerunning them.
- [ ] Run missing deck IDs from 067 down to 001, with G1 and U407 arms in parallel per deck.
- [ ] Publish a separate Full-67 diagnostic; retain U407 as an unpromoted candidate.
