# 0043 V6 U233 Restart Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resume continuous 0043 generalist PPO safely from the last complete V5 U233 model-only checkpoint after the host restart.

**Architecture:** Add a project-local V6 launch wrapper that binds the immutable U233 checkpoint and the post-U233 committed PFSP state, while reusing the audited 0043 CUDA 2.0 training path. The new repository version gets a fresh optimizer, fresh on-policy source-U233 rollout, fresh W&B identity, and untouched 2048 logical / 512 forward-microbatch semantics.

**Tech Stack:** Python 3.11, PyTorch PPO, CUDA Engine 2.0, systemd user transient service, W&B online.

## Global Constraints

- Preserve 256 games per rollout and exact 128/64/64 opponent sampling.
- Preserve focal decks 001-067 and independently sampled Policy-0809 / Champion-G1 opponents.
- Use U233 model-only checkpoint and PFSP observations committed only through source-U232.
- Reinitialize the optimizer and do not append to V5 or its W&B run.
- Keep logical minibatch 2048, forward microbatch 512, three PPO epochs, and all-checkpoint retention.
- Do not modify `engine/source/` or unrelated user changes.

---

### Task 1: V6 restart entrypoint and contract test

**Files:**
- Create: `train/0043_champion_league_rl/training/run_v6.py`
- Create: `train/0043_champion_league_rl/tests/test_v6_restart.py`

**Interfaces:**
- Consumes: `run_v1.run(...)`, V5 `update-000233.pt`, and V5 `artifact/pfsp_state.json`.
- Produces: `readiness(version_root: Path) -> dict[str, object]` and a formal V6 CLI.

- [ ] **Step 1: Write the contract test**

Assert V6 binds U233, all 67 focal decks, the V5 committed PFSP boundary, fresh optimizer, logical batch 2048, and forward microbatch 512.

- [ ] **Step 2: Run the focused test and verify it fails**

Run: `pytest -q train/0043_champion_league_rl/tests/test_v6_restart.py`
Expected: import failure because `run_v6.py` does not exist.

- [ ] **Step 3: Implement the V6 wrapper**

Implement strict checkpoint schema/update/shape checks, PFSP last-update validation, unused-version rejection, CUDA 2.0 artifact checks, a unique W&B run ID, and delegation to `run(...)` with `forward_microbatch_size=512`.

- [ ] **Step 4: Run focused regression tests**

Run: `pytest -q train/0043_champion_league_rl/tests/test_v6_restart.py train/0043_champion_league_rl/tests/test_v5_memory_safe.py train/0043_champion_league_rl/tests/test_training_regression.py`
Expected: PASS.

### Task 2: Formal launch and first-update health gate

**Files:**
- Create at runtime: `rl_runs/0043_champion_league_rl/versions/V6_generalist_focal_001_067_u233_restart/`

**Interfaces:**
- Consumes: V6 readiness result and idle RTX 5080.
- Produces: continuous user service, W&B run, U233 seed checkpoint, then U234+ metrics/checkpoints.

- [ ] **Step 1: Run readiness and confirm the target version is unused**

Run the V6 module without `--launch-formal` and save its report under `.tmp/evaluation/0043_v6_restart/`.

- [ ] **Step 2: Start continuous training as a transient user service**

Set `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`, use W&B online, and omit `--updates` so training remains continuous.

- [ ] **Step 3: Observe the first complete update**

Verify 256 valid terminal games, U234 checkpoint and metric row, zero routing failures, zero feature D2H, finite PPO metrics, healthy KL, and active CUDA utilization.
