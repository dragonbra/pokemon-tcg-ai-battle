# 0042 V10 001 Grimmsnarl from V9 U90 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Start an unbounded formal V10 PPO branch from V9 update 90 using exact focal deck 001 (`Marnie's Grimmsnarl ex / Froslass`) while preserving every other V9 setting.

**Architecture:** Strict-load the V9 U90 model-only checkpoint after the trainer snapshots immutable Policy-0809/U0 as the reference-KL policy. Create a fresh optimizer, RNG stream, on-policy rollout stream, W&B run, and version directory; only the focal exact deck and runtime-derived own-archetype class change.

**Tech Stack:** Python 3, PyTorch PPO Protocol V2, resident official CUDA engine, W&B online, unittest.

## Global Constraints

- Version is `V10_marnies_grimmsnarl_ex_froslass_001`; no prior version is resumed, overwritten, or appended.
- Source is exactly V9 `checkpoint/update-000090.pt`, SHA-256 `b9d754214deb679c9ab0fb45c35ee39dfee39692b8aef73f88b06c4c1f894776`.
- Focal deck is catalog 001, ID `marnie_s_grimmsnarl_ex_froslass_c20a8a46f5c6`, exact-deck SHA-256 `c20a8a46f5c635773754f03103652f5c534b13dc622448ed2255a97234c103af`, own-archetype class 2 (`marnies_grimmsnarl_ex`).
- Preserve V9 topology and optimization: 256 games/update, 256 trajectories, 256 CUDA lanes, PPO minibatch 2048, forward microbatch 1024, 3 epochs, decoder/allocation LR `2e-5`, Policy Adapter LR `4e-5`, Value groups LR `1e-4`, and Frozen evaluation every 10 updates.
- Preserve the explicit `--allow-fp16-deployment-numeric-drift` waiver; candidate identity, FP16-storage/FP32-runtime evaluation, opponent identity, routing, and official-engine gates remain strict.
- Reference KL remains anchored to immutable original Policy-0809/U0, not V9 U90.
- Opponents remain independent full immutable `Policy-0809` over the unchanged neutral 55-deck pool.
- The run has no update limit, retains all model-only checkpoints, and stops only via `artifact/STOP_REQUESTED` after a complete update.
- Official `engine/source/` remains read-only and promotion remains human-only.

---

### Task 1: Bind exact focal deck 001

**Files:**
- Create: `train/0042_full_model_design/focal_decks/marnies_grimmsnarl_ex_froslass_001/deck.csv`
- Create: `train/0042_full_model_design/focal_decks/marnies_grimmsnarl_ex_froslass_001/manifest.json`
- Modify: `train/0042_full_model_design/tests/test_project_identity.py`

**Interfaces:**
- Consumes: immutable catalog-001 deck and custom focal `RunConfig` fields.
- Produces: exact deck/hash provenance and class-2 routing proof.

- [ ] Add the exact 60-card catalog payload and immutable provenance manifest.
- [ ] Add a V10 regression test for exact-deck routing, U90 lineage, and class 2.
- [ ] Run `python3 -m unittest train.0042_full_model_design.tests.test_project_identity -v` and require PASS.

### Task 2: Synchronize the authoritative design

**Files:**
- Modify: `experiments/0042_full_model_design/DESIGN.md`
- Modify: `experiments/0042_full_model_design/DESIGN.html`

**Interfaces:**
- Consumes: V9 U90 checkpoint identity and deck-001 classification.
- Produces: authoritative current-stage documentation matching the launched run.

- [ ] Record V10 deck ID, exact hash, class 2, and V9 U90 source hash.
- [ ] Record fresh optimizer/on-policy state and unchanged reference/opponent/evaluation contracts.
- [ ] Preserve V9 U30/U90/U92 facts as provenance rather than V10 evidence.

### Task 3: Preflight and launch the formal run

**Files:**
- Create through runner: `rl_runs/0042_full_model_design/versions/V10_marnies_grimmsnarl_ex_froslass_001/{artifact,checkpoint,tensorboard,wandb}`
- Create through monitor: `.tmp/training_monitor/0042_full_model_design/V10_marnies_grimmsnarl_ex_froslass_001/`

**Interfaces:**
- Consumes: V9 U90 checkpoint, exact deck 001, project-local Policy-0809 resolver, and formal watchdog.
- Produces: healthy unbounded V10 PPO training.

- [ ] Require unused V10 paths, matching source checkpoint hash, free GPU, disk headroom, and online W&B availability.
- [ ] Run focused identity, initialization, deployment, routing, and monitor tests.
- [ ] Launch the existing formal monitor/runner command with V9's explicit parameters, deck-001 flags, `--launch-formal`, the FP16 drift waiver, and no `--updates` limit.
- [ ] Require U0 checkpoint/evaluation followed by healthy on-policy progress with zero identity, routing, fallback, or runtime errors.
- [ ] Report watchdog/trainer PIDs, status, W&B URL, and next Frozen milestone while leaving the run active.

### Task 4: Preserve the environment-failed V10 and launch V11

**Files:**
- Preserve: `rl_runs/0042_full_model_design/versions/V10_marnies_grimmsnarl_ex_froslass_001/`
- Create through runner: `rl_runs/0042_full_model_design/versions/V11_marnies_grimmsnarl_ex_froslass_001/{artifact,checkpoint,tensorboard,wandb}`
- Modify: `experiments/0042_full_model_design/DESIGN.md`
- Modify: `experiments/0042_full_model_design/DESIGN.html`

**Interfaces:**
- Consumes: V10's fail-closed U0 evidence and the unchanged V9 U90/deck-001 configuration.
- Produces: a clean V11 formal run with complete systemd execution environment.

- [ ] Preserve V10 U0 and its W&B run; record that Frozen runtime construction failed because the transient service PATH could not resolve `g++`.
- [ ] Allocate unused V11 paths and inject both `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` and a PATH containing `/home/cyd/.local/bin` plus `/usr/lib/wsl/lib` into the long-lived service.
- [ ] Launch V11 from V9 U90 with the same deck, optimization, evaluation, waiver, and retention settings; do not resume V10.
- [ ] Require V11 U0 identity/deployment/Frozen gates and subsequent PPO progress to pass before declaring the run healthy.
