# 0044 V22 Rollout-512 Meta-Weight-3 Implementation Plan

> **For agentic workers:** Execute this plan inline in the current session. Subagent execution is not enabled for this task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Start a new formal 0044 V22 continuation from durable V21 U1 with 512 terminal rollout games per update and 3.0x opponent sampling weights for internal Own Archetype V2 Meta IDs 00, 01, 02, 03, 05, and 27.

**Architecture:** Add a version-specific runner that strict-pins the model-only V21 U1 checkpoint, starts a fresh optimizer at local U0, preserves the immutable Champion-G3 opponent policy and V21 memory controls, and changes only the explicitly requested rollout/sampling semantics. Validate the exact deterministic 512-lane schedule before formal launch, then synchronize both authoritative 0044 design documents and launch a new private W&B run.

**Tech Stack:** Python 3, PyTorch PPO, pytest, CUDA Engine 2.0, TensorBoard, W&B online.

## Global Constraints

- Do not modify `engine/source/`.
- V22 must use a new unused `rl_runs/0044_g2_dragapult_policy_option_lora/versions/V22_*` boundary and retain model-only checkpoints for every update.
- V22 must strict-load durable V21 U1 (`update-000001.pt`) and initialize a fresh optimizer; it must not append to V21.
- Rollout collection remains FP32 and uses exactly 512 terminal games per update.
- Opponent policy remains the complete immutable `Champion-G3`; focal and opponent identities remain independent.
- Internal Meta IDs `0, 1, 2, 3, 5, 27` receive weight `3.0`; every other active Meta receives the scheduler default `1.0`.
- PFSP and periodic evaluation remain disabled, matching the rollout-only lineage.
- Canonical local metrics are written before TensorBoard and W&B mirrors; W&B target is private `dragon_bra/pokemon-tcg-policy-learning`.

---

### Task 1: Specify and validate the V22 continuation contract

**Files:**
- Create: `train/0044_g2_dragapult_policy_option_lora/tests/test_v22_rollout512_meta_weight3.py`
- Create: `train/0044_g2_dragapult_policy_option_lora/training/run_v22.py`

**Interfaces:**
- Consumes: `run_v1.run(...)`, V21 memory settings, V21 U1 model-only checkpoint, and `balanced_meta_deck_schedule(...)`.
- Produces: `run_v22.readiness()` and `run_v22.launch()` with exact V22 parent, rollout, sampling, policy, and W&B contracts.

- [x] Write tests that assert the exact parent checkpoint metadata/hash, new-version/fresh-optimizer boundary, 512 games, `{0: 3.0, 1: 3.0, 2: 3.0, 3: 3.0, 5: 3.0, 27: 3.0}`, immutable Champion-G3 opponent, disabled evaluation/PFSP, and V21 memory controls.
- [x] Run `python3 -m pytest train/0044_g2_dragapult_policy_option_lora/tests/test_v22_rollout512_meta_weight3.py -q` and confirm it fails because `run_v22` does not exist.
- [x] Implement `run_v22.py` with strict checkpoint SHA/metadata validation and a unique stable W&B run ID.
- [x] Run the focused test and confirm it passes.

### Task 2: Synchronize authoritative design documentation

**Files:**
- Modify: `experiments/0044_g2_dragapult_policy_option_lora/DESIGN.md`
- Modify: `experiments/0044_g2_dragapult_policy_option_lora/DESIGN.html`

**Interfaces:**
- Consumes: the tested V22 constants and deterministic schedule audit from Task 1.
- Produces: matching human-readable current-stage, PPO, sampling, and continuation-boundary descriptions.

- [x] Add V22 as the active continuation from V21 U1, including fresh optimizer/on-policy data, 512 games, the six weighted internal Meta IDs, all other Meta weights, unchanged Champion-G3 identity, and disabled PFSP/evaluation.
- [x] Cross-check all V22 numbers and identifiers in Markdown and HTML against `run_v22.readiness()`.
- [x] Run targeted documentation assertions from the V22 test.

### Task 3: Preflight and launch formal V22 training

**Files:**
- Create at runtime: `rl_runs/0044_g2_dragapult_policy_option_lora/versions/V22_*/artifact/training_config.json`
- Create at runtime: `rl_runs/0044_g2_dragapult_policy_option_lora/versions/V22_*/artifact/status.json`
- Create at runtime: `rl_runs/0044_g2_dragapult_policy_option_lora/versions/V22_*/checkpoint/update-000000.pt`
- Create at runtime: V22 TensorBoard and W&B staging assets under the same version.

**Interfaces:**
- Consumes: `python3 -m train.0044_g2_dragapult_policy_option_lora.training.run_v22 --launch-formal` with `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`.
- Produces: a running formal V22 PPO process and auditable initial config/status/checkpoint assets.

- [x] Run `run_v22` readiness and verify the target version paths are unused, CUDA is healthy, W&B imports, and the opponent policy identity gate passes.
- [x] Run the focused V22 test plus the adjacent V19–V21 regression tests.
- [x] Launch V22 online in a durable execution session and capture its PID plus version-scoped W&B/TensorBoard runtime paths.
- [x] Verify `training_config.json`, initial `status.json`, `update-000000.pt`, process liveness, GPU residency, and W&B run identity; report any fail-closed launch error without reusing V22.
