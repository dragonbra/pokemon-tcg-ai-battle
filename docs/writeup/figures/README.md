# Planned figures

1. `semantic-policy-flow`: generate from `docs/architecture.md`; show prototype, state, option, actor, critic, and allocation paths.
2. `bc-to-rl-timeline`: use only milestones verified in `retrospective-technical-audit.md`; omit experiment numbers from the visible graphic.
3. `router-matchups`: derive from the retained V12 CUDA-2048 report and label the exact opponent/seed contract.
4. `policy-identity-bug`: contrast complete Frozen-0806 with the historical hybrid composition.
5. `adaptation-placement`: compare decoder-only, final-Option LoRA, and shared-State adaptation by gradient path; do not invent throughput ratios.

Every rendered figure must record its source file, extraction command, and output hash before publication.
