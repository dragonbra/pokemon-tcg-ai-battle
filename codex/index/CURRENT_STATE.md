# 当前状态

## 当前候选

当前迭代候选是 [`work/alakazam_v8_current/`](../../work/alakazam_v8_current/)。候选必须与最终打包输入一致，仅包含 `main.py`、`deck.csv` 和 `cg/`；说明文件统一放在 `work/docs/`。

## 已确认基线

V6 规则语义和已有 V7 评测结论是当前设计基线。历史完整提交保留在 [submission/](../../submission/)，官方结果以 [评测索引](EVALUATION.md) 指向的报告为准。

## 下一目标

完成 V8 候选整理后，使用 correctness gate、focused evaluation 和 outcome guardrail 验证一个主要假设，再决定是否晋级。
