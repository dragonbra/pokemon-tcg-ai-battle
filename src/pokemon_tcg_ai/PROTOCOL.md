# 0045 Protocol Pointer

The repository-wide authority is `docs/rl/RL_PROMOTE_CHAMPION_FROZEN_POLICY_PROTOCOL_V1.md`.

0045 adds the project-local `Benchmark-Tiny-V2` diagnostic contract: 512 greedy official-engine CUDA games, common random numbers, 16 frozen Benchmark V2 Meta classes with 32 games each, immutable Champion-G2 opponent, and `kaggle_fp16_storage_fp32_runtime_v1` focal materialization. It runs every five updates; multiples of ten are the same report with an additional periodic-evaluate role.

Tiny V2 is a training diagnostic and never substitutes for the full CUDA-2048 Promote benchmark. Promotion remains a manual decision.
