# Superseded 0015 Design: Outcome-Aware Value Foundations

This prospective value/RL design was deferred by the user before any dataset, training run,
candidate package, or evaluation was created. It is retained as a short decision record rather
than an active project design.

The current 0015 authority is
[`../0015_dragapult_conditioned_bc/DESIGN.md`](../0015_dragapult_conditioned_bc/DESIGN.md).
It focuses on high-quality construct-conditioned Dragapult behavior cloning: exact own-deck input,
single-build baselines, joint conditioned training, a no-deck ablation, and third-build few-shot
transfer. Value/RL is explicitly out of scope until that BC evidence is established.
